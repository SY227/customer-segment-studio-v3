# Source provenance

Prepared for local review on 10 September 2026.

Repository: https://github.com/SY227/customer-segment-studio-v2
Main commit read through the connected GitHub tool: `b587b86211c74cb0935d7b75eb0f8e9592aefc59` (Launch Customer Segment Studio V2).
Root tree: `8287336468b876d0b1a794eea23d2f6801c182de`.

Container network/DNS access did not allow git clone. The user's original `customer-segment-studio-v3-artisan-v1.1.1.zip` was retrieved from their Library as a transport source. Its application, docs, public assets, source and scripts were independently verified against the GitHub commit by recursively recreating Git blob/tree object hashes. Those complete subtrees match exactly. This is not an assumption that an older archive equals current main.

Metadata changed during V2 publication (README prefix, package name/version, root lock metadata and RELEASE fields) was reconstructed from the connected GitHub reads and confirmed against the exact published Git blob hashes. See `V2_GITHUB_ROOT_HASHES.json` and `V2_BASELINE_HASHES.json` for the source record.

The tracked generated `tsconfig.tsbuildinfo` cache is intentionally not shipped. It is not needed to build and is regenerated locally. Dependencies, build caches, .git, .vercel and private environment files are not included.

V2.1 changes were made only in this working copy. No remote write, branch, pull request, deployment, account change or Vercel operation was performed.

All original 35 model/texture assets, all nine character GLBs, both art licenses, original hall and avatar renderers, atmosphere layer, globals.css and atelier.css remain byte-for-byte preserved. Product controllers, input/result interfaces, presentation templates, scripts and release docs changed as described in V2_1_DECISIONS.md.
